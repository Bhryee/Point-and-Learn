name: point_and_learn
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  camera: ^0.11.2
  firebase_auth: ^5.7.0
  firebase_core: ^3.15.1
  flutter:
    sdk: flutter
  flutter_screenutil: ^5.9.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

  shared_preferences: ^2.5.3

flutter:
  uses-material-design: true

  assets:
    - media/logo.png